<script type="text/javascript" src="//cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.min.js"></script>

<div class="container">
  <div class="row">
    <div class="col-xs-4">
      <h3>ジャンル検索</h3>
      <% @genres.each do |genre| %>
        <table>
          <td><%= link_to genre.name %></td></br>
        </table>
      <% end %>
    </div>

         <p>
  			    ようこそ、ながのCakeへ！<br>
            このサイトは、ケーキ販売のECサイトになります。<br>
            会員でない方も商品の閲覧はできますが、<br>
            購入には会員登録が必要になります。
         </p>

    <h3>おすすめ商品 </h3>
    <div class="col-xs-8">
      <div id="slider">
      <% @items.each do |item| %>
        <tr>
          <td><%= link_to attachment_image_tag(item, :image, :fill, 100, 100, fallback: "no_image.jpg"), item_path(item.id) %></td>
          <td><%#= item.name %></td>
          <!--<td>￥<%#= (item.non_taxed_price*1.1).floor %></td>-->
        </tr>
      <% end %>
      </div>

      </br>
      <div id ="more"><%= link_to "もっとみる", items_path %></div>
    </div>
  </div>
</div>